<li style="list-style-type: none; text-align: left;">
	{{name}}
<br>
	<button style="margin: 10px;" class="change-eat" data-id="{{id}}" data-eaten="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}REMAKE!{{/if}}
	</button>

	<button class="delete-burg" data-id="{{this.id}}">DELETE!</button>
</li>
